apiVersion: v1
kind: Service
metadata:
  name: agentx-service
  labels: {app.kubernetes.io/name: agentx-service}
spec:
  selector: {app.kubernetes.io/name: agentx-service}
  ports:
    - {name: http, port: {{ .Values.service.port }}, targetPort: http}
