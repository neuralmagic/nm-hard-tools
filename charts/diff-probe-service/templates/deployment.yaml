apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "diffprobe.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "diffprobe.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name | quote }}
      app.kubernetes.io/component: controller
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
      labels:
        app.kubernetes.io/name: {{ include "diffprobe.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name | quote }}
        app.kubernetes.io/component: controller
    spec:
      serviceAccountName: {{ include "diffprobe.controllerServiceAccount" . }}
      {{- with .Values.nodeSelector }}
      nodeSelector: {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
{{ toYaml . | indent 8 }}
      {{- end }}
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        fsGroup: 65532
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: controller
          image: {{ include "diffprobe.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            - name: DIFF_PROBE_SERVICE_CONFIG
              value: /etc/diff-probe/config.yaml
            - name: DIFF_PROBE_API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ required "auth.existingSecret is required" .Values.auth.existingSecret | quote }}
                  key: {{ .Values.auth.secretKey | quote }}
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            exec:
              command:
                - python
                - -c
                - >-
                  import urllib.request;
                  urllib.request.urlopen('http://127.0.0.1:8080/readyz', timeout=1).read()
            periodSeconds: 10
          livenessProbe:
            exec:
              command:
                - python
                - -c
                - >-
                  import urllib.request;
                  urllib.request.urlopen('http://127.0.0.1:8080/healthz', timeout=1).read()
            periodSeconds: 20
          resources:
{{ toYaml .Values.controller.resources | indent 12 }}
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            readOnlyRootFilesystem: true
          volumeMounts:
            - name: config
              mountPath: /etc/diff-probe
              readOnly: true
            - name: results
              mountPath: /var/lib/diff-probe-results
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: config
          configMap:
            name: {{ include "diffprobe.fullname" . }}
        - name: results
          persistentVolumeClaim:
            claimName: {{ include "diffprobe.resultClaim" . }}
        - name: tmp
          emptyDir:
            sizeLimit: 256Mi
